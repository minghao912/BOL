Make the messages the user sends a different color :ooo (home page)

When you click add friend, send some different message (search page?)

When you send a message and have no group selected, it brings up an error (home page)

Change button from "create a new group" to "direct message" if only one person is selected (home page)

Add a remove group feature (home page)

When loading, instead of just showing "no groups to show", it shows "waiting for groups to load" (home page)

Don't allow duplicate groups, as in if two groups have the same people in it, don't allow another one to be created (group creation)

Overlap between group bar and the bottom profile/create group button (home page)

Message box slightly off center (home page)